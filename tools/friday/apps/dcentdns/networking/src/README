To compile:

``make''

This will produce executable tk.

Then distribute to PlanetLab nodes:
``./scrupts/distribute_code.pl -l ucb_i3 ./tests/topology.2.txt ./tests/plab-map.2.txt''

Then run:

``./scripts/liblog_start_servers.pl -l ucb_i3 ./tests/topology.2.txt ./tests/plab-map.2.txt''

The last two parameters determine number of nodes, configuration, bad guys, etc.
